const button = document.getElementById("switch");
const head = document.getElementById("switch")

button.addEventListener("change", () =>{
  document.body.classList.toggle("dark")

})







